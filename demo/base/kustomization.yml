apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base configuration for LlamaStack TrustyAI Safety Demo
# This includes all common resources that are deployment-agnostic
# GPU-specific model deployments are handled in overlays

namespace: summit-connect-2025

resources:
  # Core infrastructure
  - ../llamastack/llamastack-trustyai-fms
  - ../llamastack/playground
  - ../llamastack/shield-registration
  - ../trustyai/trustyai_fms
  - ../workbench

# Model deployments are handled in overlays to support different GPU configurations
